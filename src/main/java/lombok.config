# makes generated setters return "this" so you can chain them conveniently (eg in tests)
lombok.accessors.chain=true
# to allow Jackson to deserialize @Value
lombok.anyConstructor.addConstructorProperties=true